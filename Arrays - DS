import math
import os
import random
import re
import sys

#
# Complete the 'reverseArray' function below.
#
n= int(input().strip())

arr = list(map(int, input().rstrip().split()))

a=arr[::-1]
for i in range(len(a)):
    print(a[i],end=' ')
